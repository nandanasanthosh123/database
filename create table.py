# import mysql.connector
# mysdb=mysql.connector.connect(
# host='localhost',
# user='root',
# password='root123',
# database='nandana')
# mycursor=mysdb.cursor()
# mycursor.execute('create table doctosmart(id int auto_increment primary key,name varchar(20),age int)')
# print('created')
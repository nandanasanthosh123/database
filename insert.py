# import mysql.connector as mysql
# mysdb=mysql.connect(
# host='localhost',
# user='root',
# password='root123',
# database='nandana')
# mycursor=mysdb.cursor()
# Name=input('enter the name')
# Age=int(input('enter the number'))
# mycursor.execute('insert into doctosmart(name,age)values(%s,%s)',(Name,Age))
# mysdb.commit()
# print('done')